# Required for real AI loops. The application still runs in transparent fallback mode without it.
GOOGLE_API_KEY=
# Optional. Change to an available structured-output-capable model in your account.
GEMINI_MODEL=gemini-3.8-flash
